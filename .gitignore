# Secrets
.env
.env.local
.env.production

# Data (backed up via rclone, not git)
data/

# Vault (synced via Google Drive, not git)
vault/

# OpenClaw reference (not part of this project)
openclaw/
openclaw-with-nixox-and-vps/

# Terraform state (managed in CI cache)
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/tfplan
terraform/.terraform.lock.hcl

# Node
node_modules/
dist/

# OS
.DS_Store
Thumbs.db

# Claude Code sessions (local state)
.claude/sessions/
.claude/statsig/
